# The next two lines should produce the same AST
#return a[3]
#return a.get(3)

# The next two lines should produce the same AST
#a[b[3]] = 3
#a.set(a.get(b.get(3)), 3)
#a.set(b.get(3), 3)

# The next two lines should produce the same AST
#a = []
#a = [1, 2, 3]
b = [1, [2], [3, [4]]]
#_uuid_generic_0001_list a

# The next two lines should produce the same AST
#b = {}
#_uuid_generic_0002_dictionary b
#a = 3
#a[3:6:3] = 2
#a[1] = 1
#a[1:2] = alpha(z)
#a[:] = 0
#a[:] = b.a(1)[1]
#a = b[1:2]
#int a = 3
#a[1] = b[0]
#a[1:2] = b[0]
#a = b[2:3]
#
#a[:] = b.a(1)[a[1]]
#a[:] = 0
#a[:] = b.a()[b.a()[1]]
#a[:] = list(a)[list(b)[10]]
#a[:] = a.b()[:]
#a[:] = a[:]

#for a[:] in b:
#  a
#end

#a[1:2] = 1
#a = 1
#a[1:2:3] = ( a[:] = 3 )

#for a in b:
#end

#for a[1] in b:
#end

#for a.s()[1] in b:
#end

#for a[b[alpha(2)]] in b:
#end


#a.get(3)
#a[1]


#a[1] = a[1]
#return a[1]
#return a[1:2]

#for a[1] in b:
#  if (a[1] == 3):
#    return a[1]
#  end
#end

#a[1] = 2
